export class Room {
  constructor(public name: string, public basement: boolean, public ground: boolean, public upper: boolean, public eventCard: boolean, public omenCard: boolean, public itemCard: boolean, public topDoor: boolean, public bottomDoor: boolean, public leftDoor: boolean, public rightDoor: boolean, public text: string) {}
}
